<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">
    <com.yisa.pary.views.CustomHeadView 
        android:id="@+id/head_view"
        android:layout_width="match_parent"
        android:layout_height="@dimen/header_height_size"
        android:layout_alignParentTop="true">
    </com.yisa.pary.views.CustomHeadView>

    <LinearLayout 
        android:id="@+id/footer"
        android:layout_width="match_parent"
        android:layout_height="@dimen/header_height_size"
        android:layout_alignParentBottom="true"
        android:background="@color/head_bg"
        android:weightSum="3">
        <TextView android:id="@+id/pray_wall_title"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:gravity="center"
            android:drawableTop="@drawable/chat_blog_btn"
            android:text="@string/pray_wall_title"/>
        <TextView android:id="@+id/notification_title"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:gravity="center"
            android:text="@string/notification_title"/>
        <TextView android:id="@+id/user_center_title"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:gravity="center"
            android:text="@string/user_center_title"
            android:drawableTop="@drawable/chat_me_btn"/>
    </LinearLayout>
    <android.support.v4.view.ViewPager
        android:id="@+id/view_page"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/head_view"
        android:layout_above="@id/footer">
    </android.support.v4.view.ViewPager>
    
</RelativeLayout>
